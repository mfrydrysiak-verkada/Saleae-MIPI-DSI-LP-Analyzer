# How to build and install

Run:

```
python3 ./build_analyzer.py
```

Install (on Mac):

```
cp ./release/libMIPI_DSI_LP_Analyzer.dylib /Applications/Logic2.app/Contents/Resources/macos-arm64/Analyzers/
```

Restart Logic app. The analyzer will be available in the Analyzers tab of the Logic app.